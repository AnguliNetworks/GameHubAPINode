export const mysql = {
    host: 'localhost',
    username: 'root',
    password: 'secret',
    database: 'api'
};

export const jwtSecret = 'secret';

export const mail = {
    host: 'smtp.example.com',
    port: 465,
    secure: true,
    auth: {
        user: 'username',
        password: 'secret'
    }
};

export const imgur = {
    clientId: 'clientId'
};
